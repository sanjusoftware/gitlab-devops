api_endpoint: https://gitlab.mydomain.com/api
api_version: v4
private_token: token
projects:
  - name: valid_name
    settings:
      unsupported:
        - key: npm_token
          value: npm-token
        - key: nexus_token
          value: nexus-token
